# Professional Portfolio (Node.js + Express)

## 🔧 Setup Instructions

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm start
   ```

3. Open in browser:
   ```
   http://localhost:3000
   ```

---

This will serve your HTML/CSS portfolio with backend handling for the contact form at `/contact`.
